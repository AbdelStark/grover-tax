"""grover-tax — Stwo vs SP1+Groth16 benchmark on a fixed secp256k1 point-add ZKP.

See `SPEC.md` for the implementation contract. This package is intentionally
empty at bootstrap; subsequent issues populate the entry points referenced in
`pyproject.toml` (`gen-fixtures`, `analyze`, `plot`, `sim-check`).
"""

__all__: list[str] = []

__version__ = "0.1.0.dev0"
